import React from "react";

interface DataViewProps {}
const DataView: React.FC<DataViewProps> = (props: DataViewProps) => {
    return <div className="schedule-edit">Schedule Data View</div>;
};

export default DataView;
