export interface SopSettingParams {
    isUpload: boolean;
}

const sopSetting = {
    sop: {
        isUpload: false,
    },
    task: {
        isUpload: true,
    },
};

export default sopSetting;
