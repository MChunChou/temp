import React, {useState, useEffect} from "react";

const View: React.FC<any> = (prop: any) => {
    const [data, setData] = useState<any>([]);
    const [gridAPI, setGridAPI] = useState<any>();


    const onGridReady = () => {

    }







    return (<div>

    </div>);
}

export default View ;